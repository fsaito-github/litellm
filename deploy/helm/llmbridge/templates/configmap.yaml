apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "llmbridge.configMapName" . }}
  labels:
    {{- include "llmbridge.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
